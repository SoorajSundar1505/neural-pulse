# Copy to .env and fill in your values:
#   cp .env.example .env

OPENAI_API_KEY=sk-your-key-here
OPENAI_MODEL=gpt-4o-mini

# Optional
# NEURAL_PULSE_PROMPT=Explain how transformer models work in one sentence.
# NEURAL_PULSE_LAYERS=32
# NEURAL_PULSE_FPS=30
# NEURAL_PULSE_ANIM_MS=100
# Optional API cap — only set if you want to limit completion length (not shown in dashboard)
# NEURAL_PULSE_MAX_TOKENS=256
